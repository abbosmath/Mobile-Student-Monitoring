{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python backend/manage.py migrate && python backend/manage.py setup_teacher && (python backend/bot/bot.py &) && gunicorn --chdir backend config.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "restartPolicyType": "ON_FAILURE"
  }
}
